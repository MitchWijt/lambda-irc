exports.handler = async function(event: any) {
    // get body from event.body and JSON.parse it.
    // check if the body is in a good message format we expect.
    // create event data from the body data
    // sent event data to the eventBus using the awk/sdk
}